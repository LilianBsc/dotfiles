# dotfiles management (bare-repo git dir lives here)
/.dotfiles/

# nested git repo, synced via its own remote
/.oh-my-zsh/

# secrets and machine-local state
/.vibe/.env
/.vibe/logs/
/.vibe/vibehistory

# nvim local state (aider sessions/history)
/.config/nvim/.aider*
